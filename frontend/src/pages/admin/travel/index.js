import React from 'react';
import { Outlet } from 'react-router-dom';

const AdminTravelLayout = () => {
  
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default AdminTravelLayout;
